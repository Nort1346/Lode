services:
  streamhub:
    build: .
    container_name: streamhub
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/.data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
